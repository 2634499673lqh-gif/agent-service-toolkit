# T111 — Product login/session client and task create form

**Model:** STANDARD; **Review:** Strong Review; **Depends:** T118.

Add the thin TaskPilot client and Streamlit Product login/session/logout shell, then title/description create form. Keep credentials transient in one session; clear password input after every attempt and protected state on 401/logout/account change. Never use legacy AgentClient, AUTH_SECRET, caller IDs, query strings or caches for identity. Reconcile lost create responses by reads; never auto-retry POST.

**Acceptance:** secure login/selection/logout, allowed create fields, finite timeout/classified errors, two-session/401/selection/timeout tests and client-only import smoke.
