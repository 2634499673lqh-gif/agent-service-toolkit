# T034 — Tenant-scoped repositories and transaction boundary

Depends on: T032.

Add tenant-scoped Task and TaskRun repository/service transaction boundaries. Use server-derived `CurrentPrincipal.organization_id` in database predicates; repositories do not commit or close sessions. Enforce the frozen active-run and state invariants transactionally. TaskStep is out of scope.
