# T032 — TaskRun schema and migration

Depends on: T031.

Implement only the approved TaskRun model and migration. A run is one durable execution attempt with ordered run number and lifecycle status. Preserve at most one active (`PENDING`/`RUNNING`) run per Task transactionally and allow multiple terminal historical runs. No Idempotency-Key, TaskStep, runtime, or API.
