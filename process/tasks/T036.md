# T036 — Task create/list/get API

Depends on: T035.

Add typed Task create/list/get routes using `CurrentPrincipal`. Preserve 401/403/404 semantics and tenant predicates; organization and creator are server-derived. No `Idempotency-Key` or request-fingerprint behavior.
