# T064 — Phase 5 runtime integration implementation

Depends on: T061–T063. Implementation Model: STRONG; Review: Strong Review.

## Goal
Implement the capability/context slice in TaskRuntimeService while preserving Phase 4 lifecycle, tenant, checkpoint, retry, and replan guarantees.

## Acceptance / evidence
Focused unit and runtime integration tests; negative tenant/authority and sanitization tests; checkpoint/resume and repeated/concurrent resume evidence using the existing deterministic test setup; no new migration or public endpoint. T064 does not certify Phase 5 completion. The workflow is: T064 implementation → independent T064 Strong Review → if approved, the user performs the GitHub Desktop commit and push to `origin` → independent read-only Phase 5 Final Audit.

## Strong Review focus
Replay/duplicate-execution limitations are explicit; terminal TaskRun state cannot be overwritten; no Phase 6 side effects or HITL scope leaked in.
