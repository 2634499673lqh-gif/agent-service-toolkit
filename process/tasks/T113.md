# T113 — TaskRun start/discovery/status view

**Model:** STANDARD; **Review:** Strong Review; **Depends:** T112, T119.

Consume run discovery, start and inspect routes. Render historical runs in server order and explicit start/retry controls. Re-read after mutation; explain that start creates persisted pending/queued state and does not execute runtime. No fabricated steps, progress, result, resume, or automatic POST retry.

**Acceptance:** run history/empty state, 409 races, timeout reconciliation and no TaskStep assumptions tested.
