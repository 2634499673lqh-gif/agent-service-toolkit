# T037 — Task update/cancel API

Depends on: T036.

Add approved Task update and persistence-only cancellation routes. Enforce lifecycle conflicts, repeated-cancel return of the existing cancelled resource, and no restart after cancellation. T037 is sequential after T036.
