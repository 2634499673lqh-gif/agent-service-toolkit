# T035 — Task lifecycle transition service

Depends on: T034.

Implement service-owned Task/TaskRun transitions, including the frozen start/retry rules, QUEUED/RUNNING active-run cardinality, terminal history, and persistence-only cancellation. Invalid transitions do not mutate state; no automatic retry or external interruption is claimed.
